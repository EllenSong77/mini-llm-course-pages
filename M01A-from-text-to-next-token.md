# M1A：一段文本怎样变成学习任务

## 本节边界

这一节只回答一个问题：

> 给模型一段文本时，我们究竟要求它学习什么？

本节不涉及：

- Transformer
- Attention 或 QKV
- Embedding
- PyTorch
- GPU 和多卡训练
- 交叉熵公式

不要提前查这些内容。本节只做纸笔推理。

## 1. 原始文本

假设训练数据只有三个句子：

```text
我爱猫
我爱狗
你爱猫
```

对计算机来说，最开始它们只是字符序列。为了进行计算，我们必须决定怎样切分这些文本。

## 2. Token 是什么

Token 是模型每一步读取或预测的基本单位。

真实大模型可能把文本切成字、词的一部分、完整词、标点或字节。切分方法会在 M2 专门学习。

为了让第一课足够简单，我们暂时规定：

> 一个汉字就是一个 token。

因此：

```text
“我爱猫” -> [“我”, “爱”, “猫”]
```

这只是本节的教学规则，不代表真实大模型一定按汉字切分。

## 3. Vocabulary 是什么

Vocabulary，简称 vocab，是模型允许使用的全部 token 集合。

从三个句子中找出不重复的汉字：

```text
我、爱、猫、狗、你
```

因此这个玩具语料的词表里有 5 种 token：

```text
vocab_size = 5
```

这里的 `vocab_size` 是数出来的，因为我们采用了“每个汉字一个 token”的简单规则。在真实项目中，我们会先选择一个目标词表规模，再通过 Tokenizer 训练得到具体 token。

## 4. Token ID 是什么

计算机需要用整数表示 token。我们可以建立一张表：

```text
我 -> 0
爱 -> 1
猫 -> 2
狗 -> 3
你 -> 4
```

于是：

```text
“我爱猫” -> [0, 1, 2]
“我爱狗” -> [0, 1, 3]
“你爱猫” -> [4, 1, 2]
```

数字本身没有语义。把“我”设为 0 或 4 都可以，只要编码和解码使用同一张表。

需要区分：

```text
原始文本：我爱猫
tokens：  [“我”, “爱”, “猫”]
token IDs：[0, 1, 2]
```

## 5. 什么叫预测下一个 token

自回归语言模型看到前面的 token，然后预测下一个 token。

对“我爱猫”，可以产生：

```text
看到“我”       -> 目标是“爱”
看到“我爱”     -> 目标是“猫”
```

这里：

- 已经看到的内容叫上下文，英文是 context；
- 希望模型预测的下一个 token 叫目标，英文是 target 或 label。

另外两个句子同理：

```text
看到“我”       -> 目标是“爱”
看到“我爱”     -> 目标是“狗”

看到“你”       -> 目标是“爱”
看到“你爱”     -> 目标是“猫”
```

三个句子一共产生六个教学样本。

## 6. 为什么输出不能只是一个固定答案

观察上下文“我爱”：

```text
我爱 -> 猫
我爱 -> 狗
```

相同上下文在语料中出现两次，但下一个 token 不同。

根据这个极小语料，我们可以统计：

```text
P(猫 | 我爱) = 1 / 2
P(狗 | 我爱) = 1 / 2
```

竖线可以读作“在已经看到……的条件下”。

因此：

```text
P(猫 | 我爱)
```

表示“已经看到‘我爱’时，下一个 token 是‘猫’的概率”。

再观察“你爱”：

```text
你爱 -> 猫
```

在当前语料中只有一次，所以经验概率是：

```text
P(猫 | 你爱) = 1
```

这不表示现实世界里“你爱”后面永远只能是“猫”，只表示这个极小训练语料目前只提供了这一种观察。

## 7. 语言模型学习的核心目标

语言模型不是背诵一条绝对规则：

```text
“我爱”后面一定是“猫”
```

它要学习的是：

```text
给定当前上下文，下一个 token 的概率分布是什么？
```

写成简写：

```text
P(next token | context)
```

当词表有 5 种 token 时，模型每次都需要为这 5 种可能性给出一组概率，例如：

```text
我：0.01
爱：0.02
猫：0.47
狗：0.47
你：0.03
总和：1.00
```

这里的数字只是示例。概率最高的 token 不一定必须被选择；生成阶段可以按照概率抽样，这会在 M1E 学习。

## 8. 这件事如何产生完整句子

模型一次只预测一个 token：

```text
输入“我”     -> 预测“爱”
输入“我爱”   -> 预测“猫”
输入“我爱猫” -> 再预测下一个 token
```

把新预测的 token 接到原文本末尾，再送给模型，反复执行，就能逐步生成长文本。

大语言模型的基础训练目标，本质上仍然是这个看似简单的任务。规模变大以后，变化的是：

- 训练文本极多；
- token 切分更复杂；
- 上下文更长；
- 模型用神经网络表达非常复杂的条件概率。

## 9. 必做练习

仍然使用：

```text
我爱猫
我爱狗
你爱猫
```

请直接用文字回答，不写代码。

### 题 1

在“一个汉字一个 token”的规则下，列出词表，并写出 `vocab_size`。

### 题 2

自己重新安排一套 token ID。不要照抄手册中的编号，然后把“你爱猫”编码成 token IDs。

### 题 3

把三个句子产生的六组“上下文 -> 目标”全部列出来。

### 题 4

根据语料填写：

```text
P(爱 | 我) = ?
P(猫 | 我爱) = ?
P(狗 | 我爱) = ?
P(猫 | 你爱) = ?
```

### 题 5

为什么不能根据这个语料断言，现实中“你爱”后面永远只能出现“猫”？

### 题 6

用你自己的话解释这句话：

```text
语言模型学习 P(next token | context)
```

## 10. 提交模板

```text
题 1：

题 2：

题 3：

题 4：

题 5：

题 6：
```

## 11. 验收标准

- [ ] 能区分文本、token 和 token ID
- [ ] 能区分上下文与目标
- [ ] 能从句子拆出 next-token 训练样本
- [ ] 能用频数计算简单的条件概率
- [ ] 理解经验概率不等于现实世界的绝对事实
- [ ] 能用自己的话解释语言模型的基本学习目标

通过本节后才进入 M1B：不用神经网络，仅通过计数构建第一个字符级语言模型。
